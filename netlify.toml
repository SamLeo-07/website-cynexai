[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  SECRETS_SCAN_OMIT_KEYS = "VITE_TURSO_DATABASE_URL,VITE_TURSO_AUTH_TOKEN"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
